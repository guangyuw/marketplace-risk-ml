[pytest]
pythonpath = .
# joblib's numpy_pickle still does `array.shape = ...`, deprecated in NumPy 2.5.
# Harmless at runtime; filter so interview `pytest` output stays clean.
filterwarnings =
    ignore:Setting the shape on a NumPy array has been deprecated:DeprecationWarning:joblib.numpy_pickle
